"use client";

import GiveTestComponent from "@/components/GiveTest/GiveTestComponent";

const GiveTest = () => {
  return (
    <div>
      <GiveTestComponent />
    </div>
  );
};

export default GiveTest;
